# 佳木的待办助手 · 使用说明

一个装在手机桌面上的四象限待办工具。数据只存在你自己手机里，不上传任何服务器。

**在线地址**：https://moonlit-firefly.github.io/todo/
**当前版本**：v9

---

## 一、核心概念

这个工具把「事」分成两类，分开记录、分开统计：

| | **事项** | **习惯** |
|---|---|---|
| 是什么 | 做完就结束的事 | 需要反复做的事 |
| 在哪 | 主页（四象限） | 周期待办页 |
| 例子 | 「周五交方案」 | 「每天喝水」 |
| 统计 | 四象限分布、完成趋势、逾期率 | 打卡热力图、连续天数 |

**为什么要分开**：习惯的每次打卡如果混进事项统计，会把「净流入」「重要不紧急占比」这些指标彻底搅乱——每天打一次卡会被算成一次"新增完成"，一个月下来数字完全失真。

---

## 二、三个页面

### 1. 主页 —— 四象限

按「重要程度」和「紧急程度」两个维度分成四块：

```
        紧急              不紧急
   ┌──────────────┬──────────────┐
重要│  重要且紧急   │  重要不紧急   │
   │     ＋       │     ＋       │
   ├──────────────┼──────────────┤
不 │ 不重要但紧急  │ 不重要不紧急  │
重要│     ＋       │     ＋       │
   └──────────────┴──────────────┘
```

**怎么用**

| 操作 | 结果 |
|---|---|
| 点区块右上角的 **＋** | 新建这个区块的待办（自动带上该象限的属性） |
| 点**圆圈** | 切换完成 / 未完成 |
| 点**文字** | 打开编辑面板（改内容、属性、截止日期） |
| 点**区块标题**或「查看全部」 | 进入该区块的完整列表 |

**重要规则**：一条待办属于哪个象限，**完全由「重要 / 紧急」两个开关决定**，不是由你在哪建的。所以：
- 只有「重要且紧急」区块的 ＋ 新建的，才是重要且紧急
- 事后改了属性，它会**自动移动到对应区块**

顶部有「待完成 / 已完成」两个子页，已完成页面同样按四象限分组。

**编辑面板里可以设**：内容、重要/紧急、截止日期、提醒时间。

**截止时间可以精确到分钟**（点一下会先选日期再选时间），待办上会实时显示还剩多久：

```
⏰ 今天 18:30 · 还剩 8 小时
⏰ 明天 09:00 · 还剩 1 天 6 小时
⏰ 9/25 09:00 · 还剩 6 天
⏰ 已逾期 3 小时          ← 超时后变红
```

最后 1 小时会切成分钟（如「还剩 42 分钟」）。**已完成的待办只显示日期时间**，不再显示倒计时——都做完了，倒计时没有意义。

### ⚡ 到期自动变紧急

**距离截止不到 24 小时，待办会自动按「紧急」处理**，即使你没勾「紧急」也会跳到紧急那一侧，并且带一个「⚡ 自动紧急」标记。

举例：一条「重要 + 不紧急」、截止明天早上 8 点的待办——
- 今天上午看：在「重要不紧急」里（距截止 46 小时）
- 明天上午看：**自动跳到「重要且紧急」**（距截止 22 小时）

这是**临时判断，不改你的设置**：把截止时间改远、或者把它标记完成，它就自动回到原来的象限。编辑器里勾选「紧急」时下方会提示这条规则。

> 「提醒时间」目前只是记录下来，不会真的弹通知——见下面第六节。

### 2. 周期待办 —— 三类习惯

顶部三个子页：

| 子页 | 行为 | 适合 |
|---|---|---|
| **日度** | 每天刷新为未完成 | 每天喝水、每天读书 |
| **周期** | 你设的天数到了才刷新 | 每 3 天浇花、每 7 天复盘 |
| **长期** | **永不刷新** | 想做但当下不合适的事，以后有机会再说 |

点右上角 **＋** 新建。新建周期待办时，「重要 / 紧急」字段会自动隐藏——因为习惯不属于四象限体系。

**刷新倒计时**：每条日度和周期待办都会显示距离下次刷新还有多久：

```
每天刷新 · 还剩 8 小时
每 3 天刷新 · 还剩 2 天 6 小时
长期                          ← 永不刷新，所以没有倒计时
```

**不管完成没完成都会显示**：
- 没完成时，它告诉你「这一轮还剩多少时间」
- 已完成时，它告诉你「什么时候会重新出现在待办里」

### 3. 数据统计 —— 两套指标

顶部可切换 **近 7 天 / 30 天 / 90 天**。

#### 📋 事项统计（只算四象限待办）

| 卡片 | 看什么 |
|---|---|
| **概览** | 未完成事项、逾期数、逾期率、今日完成、今日新增、完成/新增比 |
| **四象限分布** | 当前未完成事项的构成。理想状态是「重要不紧急」占多数 |
| **完成趋势** | 每天完成了多少，按象限堆叠成柱状图 |
| **重要不紧急占比趋势** | ⭐ **最能反映规划能力**。这个比例高，说明你在做有长期价值的事 |
| **新增 vs 完成** | 看净流入。完成/新增比超过 100% 说明你在消化历史欠账 |
| **平均完成周期** | 从创建到完成要多久，按象限分组 |
| **完成时段分布** | 你一天中什么时候最能干成事，把难事排到那个时段 |
| **整体趋势** | 前后半程对比的一句话判断 |

#### 🔄 习惯统计（只算周期待办）

| 卡片 | 看什么 |
|---|---|
| **今日打卡** | 今天完成了几条习惯，进度条形式 |
| **打卡热力图** | 每天一个方块，颜色越深表示当天完成的习惯越多 |
| **习惯稳定度** | 每条习惯的实际/应完成次数、完成率、**当前连续天数、历史最长连击** |

稳定度列表里**完成率最低的排在最上面**，优先看它们。

---

## 三、刷新规则：凌晨 4 点

**「一天」的分界线是凌晨 4 点，不是 0 点。**

这意味着：

- 你晚上 11 点打卡，凌晨 2 点再打开，**仍然是已完成** —— 熬夜党不会被莫名重置
- 到了第二天凌晨 4 点，才刷新为未完成
- 「每 3 天」的周期待办也在凌晨 4 点刷新

**刷新是怎么发生的**：网页无法在后台常驻，所以用的是「惰性结算」——在你**打开应用、从后台切回、或应用开着时每分钟**检查一次。效果和 4 点自动刷新一致，只是要等你下次打开才看得到。

### 截止时间不走 4 点分界

**周期待办**的刷新走凌晨 4 点，但**截止日期走精确时刻**——你设了几点几分，就是几点几分。

设「截止 9/19 18:30」的待办，到 18:30 整就显示「已逾期」。界面的倒计时、统计算出的逾期数、自动紧急的判定，三者用的是同一个时刻，不会互相打架。

---

## 四、数据：只在你手机里

### 存在哪

手机浏览器的本地存储（localStorage）。**完全不上传**——代码里没有任何网络请求，你在飞行模式下也能正常使用。

### 三个保护机制

| 机制 | 说明 |
|---|---|
| **每日自动快照** | 每天第一次打开自动留一份，保留最近 3 份 |
| **删除可撤销** | 删掉后 6 秒内底部会弹「撤销」 |
| **损坏保护** | 万一数据读取异常，原始内容会被保留并可下载，不会被覆盖 |

### 手动备份（重要）

`⋯` → **导出备份** → 得到一个 JSON 文件，存到网盘或发给自己。

**建议每周导一次。** 因为：
- 清理浏览器缓存 → 数据没了
- 卸载浏览器 → 数据没了
- 换新手机 → 数据没了

**误删了怎么办**：`⋯` → **从自动快照恢复** → 选一份恢复。恢复前当前数据也会自动存一份，可以再退回来。

### 换手机 / 多设备

导出 JSON → 在新设备上打开同一个网址 → `⋯` → **导入备份**。

> 注意：**不支持自动同步**。手机和电脑各存各的，互不影响。

---

## 五、装到手机上

1. 手机浏览器打开 https://moonlit-firefly.github.io/todo/
2. 浏览器菜单（⋮）→ **添加到主屏幕**
3. 从桌面图标打开，**没有地址栏**，跟正常 App 一样

**验证是否正常**：
- 打开后**等 3~5 秒**再关，让离线缓存写完
- 开**飞行模式** → 打开应用 → 能正常用 = 离线缓存生效
- 新建几条 → 完全退出 → 重开 → 数据还在 = 本地存储正常

---

## 六、常见问题

**Q：为什么提醒时间不弹通知？**
A：这需要系统级权限（闹钟、状态栏通知），网页做不到。目前的替代方案是把截止日期记下来。**.ics 日历导出**是规划中的功能——导出后导入手机系统日历，闹钟、锁屏提醒、状态栏就都由系统负责了。

**Q：为什么统计页刚打开时数字很少？**
A：数据是**从现在开始记录**的。历史只能靠已有待办补出「创建」和「完成」两个时间点。用得越久，趋势越有意义。

**Q：改了名/换了图标怎么办？**
A：桌面图标的名字可能要过一阵才更新（安卓的已安装应用有独立缓存）。等不及就长按图标删掉，重新「添加到主屏幕」。**数据不会丢**，数据跟着网址走，跟图标无关。

**Q：习惯的连续天数怎么算的？**
A：今天还没打卡**不算断连**（这一天还没过完）；断档超过一天才归零，但「历史最长」会一直保留。

**Q：清空数据后还能找回吗？**
A：能。清空前会自动存一份快照，用 `⋯` → 从自动快照恢复 可以找回。

**Q：为什么待办会自己跳到「重要且紧急」？**
A：这是**到期自动紧急**。距离截止不到 24 小时就会自动晋级，条目上会显示「⚡ 自动紧急」。把截止时间改远或标记完成，它就会自动回到原来的象限。详见第二节。

**Q：屏幕上方/下方有空白，页面没铺满怎么办？**
A：先到 `⋯` → **显示屏幕诊断信息**，把弹出来的内容发我。那种数字能直接定位是「安全区重复叠加」「视口高度算错」还是「独立窗口模式没生效」。

**Q：为什么「每 3 天」的倒计时是「2 天 18 小时」这种怪数字？**
A：因为周期待办在**凌晨 4 点**对齐，不是按你创建它的时刻。刚刷新时距下一个刷新点就是 2 天多，不是整 3 天。

---

## 七、更新应用

如果你自己改了代码，或者我给了新版本：

1. 打开 GitHub 仓库 https://github.com/moonlit-firefly/todo
2. **Add file** → **Upload files**
3. 把改动过的文件拖进去覆盖（通常是 `index.html` 和 `sw.js`）
4. **Commit changes**
5. 等约 10 分钟（GitHub 有 CDN 缓存）
6. 手机上打开应用 → 完全关掉 → 再打开

> `sw.js` 里的 `VERSION` 每版都会加一（现在是 `todo-v7`），这是让旧缓存失效的开关。**改代码时如果不改这个版本号，手机会一直看到旧版本。**
> 更新只换代码，**不影响你的待办数据**。

---

## 八、技术说明

| 项 | 值 |
|---|---|
| 形态 | 单页 PWA，纯静态，无后端 |
| 大小 | 6 个文件 / 约 67 KB |
| 依赖 | 无。图表是手写的内联 SVG/CSS |
| 离线 | Service Worker 缓存优先，秒开 |
| 数据 | localStorage，键名 `dsh.todo.v2` |
| 时间基准 | 凌晨 4 点（`DAY_OFFSET`） |
| 统计基础 | 追加式事件日志（`db.log`）+ 待办自身的完成计数器 |

### 文件清单

```
index.html            应用主体（界面 + 逻辑，全内联）
sw.js                 Service Worker（离线缓存）
manifest.json         PWA 清单（应用名、图标、独立窗口）
icon-192.png          图标
icon-512.png          图标
apple-touch-icon.png  iOS 图标
```
